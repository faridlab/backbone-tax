-- Migration: Create tax_categories table
-- Generated by metaphor-schema

-- Create tax_kind enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'tax_kind') THEN
        CREATE TYPE tax_kind AS ENUM ('vat', 'withholding', 'sales', 'other');
    END IF;
END
$$;

-- Create tax_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'tax_status') THEN
        CREATE TYPE tax_status AS ENUM ('active', 'inactive');
    END IF;
END
$$;

CREATE SCHEMA IF NOT EXISTS tax;

CREATE TABLE IF NOT EXISTS tax.tax_categories (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    code TEXT NOT NULL,
    name TEXT NOT NULL,
    tax_kind tax_kind NOT NULL DEFAULT 'vat',
    status tax_status NOT NULL DEFAULT 'active',
    metadata JSONB NOT NULL DEFAULT '{"created_at":null,"updated_at":null,"deleted_at":null,"created_by":null,"updated_by":null,"deleted_by":null}'::jsonb,
    PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_tax_categories_code ON tax.tax_categories (code) WHERE (metadata->>'deleted_at') IS NULL;

CREATE INDEX IF NOT EXISTS idx_tax_categories_tax_kind_status ON tax.tax_categories (tax_kind, status);

-- GIN index for audit metadata JSONB queries
CREATE INDEX IF NOT EXISTS idx_tax_categories_metadata_gin ON tax.tax_categories USING GIN (metadata);
CREATE INDEX IF NOT EXISTS idx_tax_categories_metadata_deleted_at ON tax.tax_categories ((metadata->>'deleted_at'));
CREATE INDEX IF NOT EXISTS idx_tax_categories_metadata_created_at ON tax.tax_categories ((metadata->>'created_at'));
CREATE INDEX IF NOT EXISTS idx_tax_categories_metadata_updated_at ON tax.tax_categories ((metadata->>'updated_at'));

-- Triggers for automatic metadata timestamp management
-- Automatically sets created_at on INSERT and updated_at on UPDATE

-- Function to set metadata->'created_at' on INSERT
CREATE OR REPLACE FUNCTION tax.tax_categories_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS tax_categories_insert_audit ON tax.tax_categories;
CREATE TRIGGER tax_categories_insert_audit BEFORE INSERT ON tax.tax_categories
    FOR EACH ROW EXECUTE FUNCTION tax.tax_categories_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS tax_categories_update_audit ON tax.tax_categories;
CREATE TRIGGER tax_categories_update_audit BEFORE UPDATE ON tax.tax_categories
    FOR EACH ROW EXECUTE FUNCTION tax.tax_categories_audit_timestamp();
