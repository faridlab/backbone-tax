//! Repository trait for TaxTemplate aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the TaxTemplate aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{TaxTemplate, TaxStatus, TemplateType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTemplatePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl TaxTemplatePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct TaxTemplatePaginatedResult {
    pub data: Vec<TaxTemplate>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTemplateFilter {
    pub code: Option<String>,
    pub name: Option<String>,
    pub template_type: Option<TemplateType>,
    pub tax_category_id: Option<Uuid>,
    pub is_inclusive: Option<bool>,
    pub status: Option<TaxStatus>,
}

impl TaxTemplateFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.code.is_some() || self.name.is_some() || self.template_type.is_some() || self.tax_category_id.is_some() || self.is_inclusive.is_some() || self.status.is_some()
    }
}

/// Repository trait for TaxTemplate aggregate
///
/// This trait defines the persistence contract for TaxTemplate entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait TaxTemplateRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new tax_template entity
    async fn save(&self, entity: &TaxTemplate) -> Result<TaxTemplate>;

    /// Find tax_template by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<TaxTemplate>>;

    /// Find all tax_template entities
    async fn find_all(&self) -> Result<Vec<TaxTemplate>>;

    /// Update tax_template by ID
    async fn update(&self, id: &str, entity: &TaxTemplate) -> Result<Option<TaxTemplate>>;

    /// Delete tax_template by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List tax_template with pagination
    async fn list(&self, params: TaxTemplatePaginationParams) -> Result<TaxTemplatePaginatedResult>;

    /// List tax_template with pagination and filters
    async fn list_with_filters(&self, params: TaxTemplatePaginationParams, filters: TaxTemplateFilter) -> Result<TaxTemplatePaginatedResult>;

    /// Count all tax_template entities
    async fn count(&self) -> Result<u64>;

    /// Count tax_template entities matching filters
    async fn count_with_filters(&self, filters: TaxTemplateFilter) -> Result<u64>;

    /// Check if tax_template exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete tax_template by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted tax_template by ID
    async fn restore(&self, id: &str) -> Result<Option<TaxTemplate>>;

    /// List soft-deleted tax_template entities
    async fn list_deleted(&self, params: TaxTemplatePaginationParams) -> Result<TaxTemplatePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save tax_template entities
    async fn bulk_save(&self, entities: &[TaxTemplate]) -> Result<Vec<TaxTemplate>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
