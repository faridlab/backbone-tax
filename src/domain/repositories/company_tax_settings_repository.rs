//! Repository trait for CompanyTaxSettings aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the CompanyTaxSettings aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::{CompanyTaxSettings, TaxExigibility, TaxRoundingMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CompanyTaxSettingsPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl CompanyTaxSettingsPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct CompanyTaxSettingsPaginatedResult {
    pub data: Vec<CompanyTaxSettings>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CompanyTaxSettingsFilter {
    pub company_id: Option<Uuid>,
    pub rounding_method: Option<TaxRoundingMethod>,
    pub default_exigibility: Option<TaxExigibility>,
    pub cash_basis_transition_account_id: Option<Uuid>,
}

impl CompanyTaxSettingsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some()
            || self.rounding_method.is_some()
            || self.default_exigibility.is_some()
            || self.cash_basis_transition_account_id.is_some()
    }
}

/// Repository trait for CompanyTaxSettings aggregate
///
/// This trait defines the persistence contract for CompanyTaxSettings entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait CompanyTaxSettingsRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new company_tax_settings entity
    async fn save(&self, entity: &CompanyTaxSettings) -> Result<CompanyTaxSettings>;

    /// Find company_tax_settings by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<CompanyTaxSettings>>;

    /// Find all company_tax_settings entities
    async fn find_all(&self) -> Result<Vec<CompanyTaxSettings>>;

    /// Update company_tax_settings by ID
    async fn update(
        &self,
        id: &str,
        entity: &CompanyTaxSettings,
    ) -> Result<Option<CompanyTaxSettings>>;

    /// Delete company_tax_settings by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List company_tax_settings with pagination
    async fn list(
        &self,
        params: CompanyTaxSettingsPaginationParams,
    ) -> Result<CompanyTaxSettingsPaginatedResult>;

    /// List company_tax_settings with pagination and filters
    async fn list_with_filters(
        &self,
        params: CompanyTaxSettingsPaginationParams,
        filters: CompanyTaxSettingsFilter,
    ) -> Result<CompanyTaxSettingsPaginatedResult>;

    /// Count all company_tax_settings entities
    async fn count(&self) -> Result<u64>;

    /// Count company_tax_settings entities matching filters
    async fn count_with_filters(&self, filters: CompanyTaxSettingsFilter) -> Result<u64>;

    /// Check if company_tax_settings exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete company_tax_settings by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted company_tax_settings by ID
    async fn restore(&self, id: &str) -> Result<Option<CompanyTaxSettings>>;

    /// List soft-deleted company_tax_settings entities
    async fn list_deleted(
        &self,
        params: CompanyTaxSettingsPaginationParams,
    ) -> Result<CompanyTaxSettingsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save company_tax_settings entities
    async fn bulk_save(&self, entities: &[CompanyTaxSettings]) -> Result<Vec<CompanyTaxSettings>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
