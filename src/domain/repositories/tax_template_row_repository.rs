//! Repository trait for TaxTemplateRow aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the TaxTemplateRow aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{TaxTemplateRow, ChargeType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTemplateRowPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl TaxTemplateRowPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct TaxTemplateRowPaginatedResult {
    pub data: Vec<TaxTemplateRow>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TaxTemplateRowFilter {
    pub template_id: Option<Uuid>,
    pub charge_type: Option<ChargeType>,
    pub account_id: Option<Uuid>,
    pub is_withholding: Option<bool>,
    pub description: Option<String>,
}

impl TaxTemplateRowFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.template_id.is_some() || self.charge_type.is_some() || self.account_id.is_some() || self.is_withholding.is_some() || self.description.is_some()
    }
}

/// Repository trait for TaxTemplateRow aggregate
///
/// This trait defines the persistence contract for TaxTemplateRow entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait TaxTemplateRowRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new tax_template_row entity
    async fn save(&self, entity: &TaxTemplateRow) -> Result<TaxTemplateRow>;

    /// Find tax_template_row by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<TaxTemplateRow>>;

    /// Find all tax_template_row entities
    async fn find_all(&self) -> Result<Vec<TaxTemplateRow>>;

    /// Update tax_template_row by ID
    async fn update(&self, id: &str, entity: &TaxTemplateRow) -> Result<Option<TaxTemplateRow>>;

    /// Delete tax_template_row by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List tax_template_row with pagination
    async fn list(&self, params: TaxTemplateRowPaginationParams) -> Result<TaxTemplateRowPaginatedResult>;

    /// List tax_template_row with pagination and filters
    async fn list_with_filters(&self, params: TaxTemplateRowPaginationParams, filters: TaxTemplateRowFilter) -> Result<TaxTemplateRowPaginatedResult>;

    /// Count all tax_template_row entities
    async fn count(&self) -> Result<u64>;

    /// Count tax_template_row entities matching filters
    async fn count_with_filters(&self, filters: TaxTemplateRowFilter) -> Result<u64>;

    /// Check if tax_template_row exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete tax_template_row by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted tax_template_row by ID
    async fn restore(&self, id: &str) -> Result<Option<TaxTemplateRow>>;

    /// List soft-deleted tax_template_row entities
    async fn list_deleted(&self, params: TaxTemplateRowPaginationParams) -> Result<TaxTemplateRowPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save tax_template_row entities
    async fn bulk_save(&self, entities: &[TaxTemplateRow]) -> Result<Vec<TaxTemplateRow>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
