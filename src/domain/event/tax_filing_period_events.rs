//! Domain events for TaxFilingPeriod entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::TaxFilingPeriod;
use backbone_messaging::CrudEvent;

/// Domain events for TaxFilingPeriod — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<TaxFilingPeriod>)` variant.
pub type TaxFilingPeriodEvent = CrudEvent<TaxFilingPeriod>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for TaxFilingPeriod events.
pub type TaxFilingPeriodEventPublisher = Arc<dyn CrudEventPublisher<TaxFilingPeriod> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTaxFilingPeriodEventPublisher = NoOpCrudEventPublisher<TaxFilingPeriod>;
