//! Auth configuration for EFakturDocument handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EFakturDocument>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EFakturDocumentService;
use crate::domain::entity::EFakturDocument;

/// Permission string constants for EFakturDocument operations.
pub mod permissions {
    pub const LIST:        &str = "e_faktur_document:list";
    pub const READ:        &str = "e_faktur_document:read";
    pub const CREATE:      &str = "e_faktur_document:create";
    pub const UPDATE:      &str = "e_faktur_document:update";
    pub const DELETE:      &str = "e_faktur_document:delete";
    pub const RESTORE:     &str = "e_faktur_document:restore";
    pub const TRASH:       &str = "e_faktur_document:trash";
    pub const EMPTY_TRASH: &str = "e_faktur_document:empty_trash";
    pub const BULK_CREATE: &str = "e_faktur_document:bulk_create";
    pub const UPSERT:      &str = "e_faktur_document:upsert";
}

/// Resource policy for EFakturDocument — maps CRUD actions to permission strings.
pub struct EFakturDocumentPolicy;

#[async_trait]
impl ResourcePolicy<EFakturDocument> for EFakturDocumentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EFakturDocument,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for EFakturDocument handlers.
///
/// Wraps `EFakturDocumentService` and checks `EFakturDocumentPolicy` before delegating.
pub type EFakturDocumentGuard = PermissionGuard<EFakturDocument>;

/// Service-integrated permission guard for EFakturDocument (for use in generated handlers).
pub type EFakturDocumentServiceGuard = ServicePermissionGuard<EFakturDocument, EFakturDocumentService, EFakturDocumentPolicy>;

// <<< CUSTOM
// END CUSTOM
