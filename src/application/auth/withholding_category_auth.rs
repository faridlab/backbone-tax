//! Auth configuration for WithholdingCategory handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<WithholdingCategory>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WithholdingCategoryService;
use crate::domain::entity::WithholdingCategory;

/// Permission string constants for WithholdingCategory operations.
pub mod permissions {
    pub const LIST:        &str = "withholding_category:list";
    pub const READ:        &str = "withholding_category:read";
    pub const CREATE:      &str = "withholding_category:create";
    pub const UPDATE:      &str = "withholding_category:update";
    pub const DELETE:      &str = "withholding_category:delete";
    pub const RESTORE:     &str = "withholding_category:restore";
    pub const TRASH:       &str = "withholding_category:trash";
    pub const EMPTY_TRASH: &str = "withholding_category:empty_trash";
    pub const BULK_CREATE: &str = "withholding_category:bulk_create";
    pub const UPSERT:      &str = "withholding_category:upsert";
}

/// Resource policy for WithholdingCategory — maps CRUD actions to permission strings.
pub struct WithholdingCategoryPolicy;

#[async_trait]
impl ResourcePolicy<WithholdingCategory> for WithholdingCategoryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &WithholdingCategory,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for WithholdingCategory handlers.
///
/// Wraps `WithholdingCategoryService` and checks `WithholdingCategoryPolicy` before delegating.
pub type WithholdingCategoryGuard = PermissionGuard<WithholdingCategory>;

/// Service-integrated permission guard for WithholdingCategory (for use in generated handlers).
pub type WithholdingCategoryServiceGuard = ServicePermissionGuard<WithholdingCategory, WithholdingCategoryService, WithholdingCategoryPolicy>;

// <<< CUSTOM
// END CUSTOM
