//! Auth configuration for TaxTransaction handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<TaxTransaction>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::TaxTransactionService;
use crate::domain::entity::TaxTransaction;

/// Permission string constants for TaxTransaction operations.
pub mod permissions {
    pub const LIST:        &str = "tax_transaction:list";
    pub const READ:        &str = "tax_transaction:read";
    pub const CREATE:      &str = "tax_transaction:create";
    pub const UPDATE:      &str = "tax_transaction:update";
    pub const DELETE:      &str = "tax_transaction:delete";
    pub const RESTORE:     &str = "tax_transaction:restore";
    pub const TRASH:       &str = "tax_transaction:trash";
    pub const EMPTY_TRASH: &str = "tax_transaction:empty_trash";
    pub const BULK_CREATE: &str = "tax_transaction:bulk_create";
    pub const UPSERT:      &str = "tax_transaction:upsert";
}

/// Resource policy for TaxTransaction — maps CRUD actions to permission strings.
pub struct TaxTransactionPolicy;

#[async_trait]
impl ResourcePolicy<TaxTransaction> for TaxTransactionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &TaxTransaction,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for TaxTransaction handlers.
///
/// Wraps `TaxTransactionService` and checks `TaxTransactionPolicy` before delegating.
pub type TaxTransactionGuard = PermissionGuard<TaxTransaction>;

/// Service-integrated permission guard for TaxTransaction (for use in generated handlers).
pub type TaxTransactionServiceGuard = ServicePermissionGuard<TaxTransaction, TaxTransactionService, TaxTransactionPolicy>;

// <<< CUSTOM
// END CUSTOM
