//! Auth configuration for TaxFilingPeriod handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<TaxFilingPeriod>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::TaxFilingPeriodService;
use crate::domain::entity::TaxFilingPeriod;

/// Permission string constants for TaxFilingPeriod operations.
pub mod permissions {
    pub const LIST:        &str = "tax_filing_period:list";
    pub const READ:        &str = "tax_filing_period:read";
    pub const CREATE:      &str = "tax_filing_period:create";
    pub const UPDATE:      &str = "tax_filing_period:update";
    pub const DELETE:      &str = "tax_filing_period:delete";
    pub const RESTORE:     &str = "tax_filing_period:restore";
    pub const TRASH:       &str = "tax_filing_period:trash";
    pub const EMPTY_TRASH: &str = "tax_filing_period:empty_trash";
    pub const BULK_CREATE: &str = "tax_filing_period:bulk_create";
    pub const UPSERT:      &str = "tax_filing_period:upsert";
}

/// Resource policy for TaxFilingPeriod — maps CRUD actions to permission strings.
pub struct TaxFilingPeriodPolicy;

#[async_trait]
impl ResourcePolicy<TaxFilingPeriod> for TaxFilingPeriodPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &TaxFilingPeriod,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for TaxFilingPeriod handlers.
///
/// Wraps `TaxFilingPeriodService` and checks `TaxFilingPeriodPolicy` before delegating.
pub type TaxFilingPeriodGuard = PermissionGuard<TaxFilingPeriod>;

/// Service-integrated permission guard for TaxFilingPeriod (for use in generated handlers).
pub type TaxFilingPeriodServiceGuard = ServicePermissionGuard<TaxFilingPeriod, TaxFilingPeriodService, TaxFilingPeriodPolicy>;

// <<< CUSTOM
// END CUSTOM
