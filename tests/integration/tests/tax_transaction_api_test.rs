//! TaxTransaction API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the TaxTransaction CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// TAXTRANSACTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for TaxTransaction
pub struct TaxTransactionTestData;

impl TestDataGenerator for TaxTransactionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "invoice_ref": Uuid::new_v4().to_string(),
            "invoice_kind": "sales",
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "taxable_base": 0,
            "output_total": 0,
            "input_total": 0,
            "withholding_total": 0,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "source": "seam",
            "efaktur_document_id": null,
            "status": "recorded",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "invoice_ref": Uuid::new_v4().to_string(),
            "invoice_kind": "sales",
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "taxable_base": 0,
            "output_total": 0,
            "input_total": 0,
            "withholding_total": 0,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "source": "seam",
            "efaktur_document_id": null,
            "status": "recorded",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// TAXTRANSACTION API TEST
// ============================================================================

/// TaxTransaction API test suite
pub struct TaxTransactionApiTest {
    inner: GenericCrudTest<TaxTransactionTestData>,
}

impl TaxTransactionApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/tax_transactions", "TaxTransaction");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, TaxTransactionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for TaxTransactionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_tax_transaction_crud() {
        let mut test = TaxTransactionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add TaxTransaction specific tests here
// <<< CUSTOM TESTS END >>>
